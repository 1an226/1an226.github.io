backend:
  name: github
  repo: 1an226/1an226.github.io
  branch: main
  auth_type: implicit
  site_domain: "1an226.github.io"

media_folder: "assets/images/uploads"
public_folder: "/assets/images/uploads"

collections:
  # Remove genres collection - Paul shouldn't edit genre descriptions
  - name: "posts"
    label: "Blog Posts"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Genre", name: "genre", widget: "select", 
         options: ["cyberpunk", "horror", "modern-warfare", "philosophy", "politics", "talk-of-town"]}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Featured Image", name: "image", widget: "image", required: false}
      - {label: "Content", name: "body", widget: "markdown"}
